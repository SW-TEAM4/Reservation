<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.team4.model.mapper.LodFacilityMapper">
    <select id="facilityList" parameterType="kr.co.team4.model.dto.LodFacilityDTO" resultType="kr.co.team4.model.dto.LodFacilityDTO">
        SELECT L.lod_name,
               (SELECT GROUP_CONCAT(F.lod_facility_name)
                FROM LOD_FACILITY F
                WHERE F.LOD_FACILITY_IDX IN
                      (SELECT SERVICE_IDX
                       FROM LODGMENT_MAPPING_SERVICE LMS
                       WHERE LMS.LOD_IDX = L.LOD_IDX)
               ) AS FACILITIES
        FROM LODGMENT L
        WHERE L.LOD_IDX = #{lod_idx};
    </select>
</mapper>
